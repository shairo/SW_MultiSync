# 目的と設計 — 車両位置同期の改善

このフックアプリの**最終目的**と、それを実現するための設計方針・検証項目をまとめる。
ワイヤ仕様の詳細は [protocol.md](protocol.md)、採取手順は [収集シナリオ.md](収集シナリオ.md) を参照。

## 1. 解決したい問題

Stormworksのマルチプレイにおける、**プレイヤーから離れた車両の位置・姿勢の表示遅延**。

- サーバーはワールドtickごとにクライアントへ同期メッセージ(`msgType=8`)を送る。メッセージは
  **tickと1:1**（メッセージ自体の増減は不可）。この中に**間欠的に**車両同期レコード(`0x81`)が載る。
- 同期頻度は車両ごと・プレイヤーごとに独立。**プレイヤーに近い車両ほど高頻度、遠い車両ほど低頻度**。
- クライアントは車両位置の**未来推定（外挿）をしない**。同期レコードを受け取ると、
  「送信間隔時間をかけて、レコードの位置・姿勢へ到達する」挙動をとる（＝補間はするが予測はしない）。
- 結果、5秒間隔でしか同期されない遠方車両は、**クライアント上で最大5秒遅れた位置**に見える。
  対戦系マルチではこの遅延が体験を著しく損なう。

### 挙動の裏付け（前プロトコルでの検証）
- **ETAを過ぎても次の同期レコードが来ないと、その車両はクライアント上で完全停止**する。
  → レコードに速度が入っておらず、クライアントも疑似速度を計算していない証拠。
- 近距離車両の同期間隔は **5 tick**。

## 2. 0x81 の time フィールド = 適用ETA（未来ワールドtick）

```
u32 tag = 0x81
u32 vehicleId
u32 time        ← このレコードの位置・姿勢に「到達すべき」未来のワールドtick（ETA）
u16 bodyCount
Body[bodyCount] ...
```

- エンベロープの `tick`(+8) = そのメッセージのワールド時刻（現在）。
- `0x81.time` = **その位置・姿勢に到達させる目標ワールドtick（未来）**。
- 近距離車両では **`time = メッセージのワールドtick + 5`**（間隔5tickぶん先）。
- レコードのデータは**その時点の最新値**という前提。

> ✅ **実データで確定済み（`swcap eta`）**:
> - delta = `record.time − envelope.tick` は**常に ≥ 0**（過去tickは無い）。combat 59,772件・S5 518件で例外なし。
> - **S5(近距離・単一車両)は全518件でdelta=+5**、かつ実間隔も5tick → `time = tick + interval`、近距離間隔=5tick。
> - **combat(遠方車両)はdelta=5〜約687tick**に広がり、delta分布が(宛先,車両)ごとの実間隔分布と一致 →
>   `time = tick +（次同期までの間隔）`。遠方ほど間隔が大きい=低頻度。
> - tick rate ≈ **62.5 tick/秒(≒60)**。近距離=約80ms遅延、delta約300tick≈**5秒** → 報告どおりの遅延を裏付け。
>
> **横流し時のtime書き換え式**: 宛先Bへ新鮮な0x81を差し込むときは `time = B_message.tick + 希望間隔(例:5)`
> にする。Bの古い長間隔ではなく短い未来窓で補間させる。

## 3. 改善の中核アイデア — クライアント間レコード横流し（cross-feed）

**前提となる好条件**: `SendMessageToUser` は **宛先ごとに1回ずつ**呼ばれる（vtable[0]）。
つまりフックは**全プレイヤー宛の全ストリームを1プロセス内で観測できる**。ここに、ある車両について
「最も高頻度に更新されているストリーム」の最新 `0x81` が常に見えている。

**やること**:
- ある車両 V の同期レコードが**誰か（近くにいるプレイヤー A宛）**に流れた瞬間、その最新レコードを
  グローバルに保持する（車両ごとに latest-0x81 をキャッシュ）。
- V が**遠い他プレイヤー B** には低頻度でしか送られていない場合、B宛のストリームに
  V のレコードを**高頻度で横流し（追記/注入）**して、Bの画面上でも V を高頻度更新にする。

**time の書き換えが必要**:
- 横流し先 B に対しては、B宛メッセージの現在ワールドtick + 適切な間隔（例: 5）へ `time` を
  付け替える必要がある。**Bにとっての未来tick**でなければ正しく補間されない。
- サーバーが従来どおり B に送る分の `0x81` も、同様に time が B基準になっている点に注意。

## 4. 設計方針（フック実装 ③編集・注入フェーズ）

### 4.1 注入は「新規メッセージ」ではなく「既存tickメッセージへの追記」を第一候補にする
理由: 新しいワールドtickを持つメッセージを差し込むと**ワールド時刻の連続性が崩れ**、
クライアントが再送要求を出す恐れがある（§5）。**そのtickで自然に出ていくメッセージの
`records[]` に 0x81 を追記**すれば、新tickを増やさずに済む。
（追記に伴い `recordCount` と本体長を整合させる。0x81は長さ確定済みなので安全。）

### 4.2 車両ごと latest-0x81 キャッシュ
- キー: vehicleId。値: 最新の 0x81 本体（body群）＋観測ワールドtick＋観測元。
- どのストリームで観測しても更新（全宛先を横断で見られる利点）。

### 4.3 宛先ごとの送出制御
- 宛先プレイヤー B ×車両 V ごとに「最後に V を B へ送ったワールドtick」を記録し、
  **既にサーバーが高頻度で送っている車両には横流ししない**（二重更新の回避）。
- 横流しする場合のみ time を B基準へ書き換えて追記。

### 4.4 tick計算はエンベロープの world-tick のみを使う
- サーバーは**リアル時間で約62tps**、ゲーム内1秒=60tick。負荷（多数車両等）でtpsは落ち込む。
- したがって **ETA等のtick計算にリアル時間を使ってはいけない**。全て**メッセージの world-tick 差分**で行う。
- リアル時間は**タイムアウト等の例外処理限定**（例: 「一定リアル秒どの宛先にもVの新鮮なレコードが来ない
  ＝供給停止」の異常検知。tick自体が進まない状況の検出）。

## 4.5 ETA設計 — 遅延短縮と「停止」回避の両立

> **注記（最終方針は §4.10）**: 本節は初期の「ETA短縮＋供給断時のランプアップ/ハンドオフ(item 6)」案。
> その後、**ETAは保持し pose=predict(ETA−Λ) を入れる**方式（§4.10）に置き換わり、ETAがサーバーの次回送出時刻の
> ままなのでフリーズが構造的に起きず、item 6 のランプアップは不要になった。以下は経緯として残す。

**再解釈（重要）**: 実プレイで遠方車両が完全停止して見えないということは、`time`(ETA) は
**サーバーが「次の同期を送る時刻」を予言し、常にその約束を守っている**ことを意味する。
よって滑らかさの条件は「**守れるETAしか約束しない**」こと。横流しでもこれを守る。

### 停止回避の基本プリミティブ: ハード停止 → 緩やかな減速
ETA超過での完全停止は避けられる。**次の新鮮なレコードが間に合わない時、直前のposeのまま ETA だけ
延長したレコードを再送する**（B宛メッセージは毎tick出るので追記/書き換えコストはゼロ）。
- poseが同じなので「ガクッと固まる」ではなく「緩やかに減速して止まる」挙動になる。
- 供給復帰で通常の高頻度更新へ自然復帰。

### ETAの決め方（遅延最小化）
供給源はVの近くにいるピアAのストリーム＝**Aの受信間隔が横流しできる間隔**。サーバーと同じ発想で:
```
inject時 ETA = 現tick + s_hat + margin
  s_hat  = 最近のVの供給間隔のロバスト推定（直近N件の最大 or p90）
  margin = 1〜2 tick（ジッタ吸収）
  上限   = そのV→Bのネイティブ間隔（改善はしても悪化はさせない）
```
- Aが近い(5tick)ならBも **ETA+5 ≒ 80ms** に短縮。
- **s_hat=実測間隔にすると補間速度=実速度**になり velocity の見た目も正しくなる。
- 供給が来るたび新レコードで**リターゲット(refresh)** → 通常時はETA到達前に必ず更新が入り止まらない。

### 供給停止/低下時のフォールバック（停止しない不変条件）
**不変条件: 最後に送ったレコードのETAが切れる前に必ず何か届く。**
- 供給が遅れ始めたら inject のたびに **s_hat を上げてETAを徐々に延長** → 完全停止時点で長ETAを渡し済みにし、
  **ネイティブのサーバーレコードへ主導権を返す**（短い約束を残さない）。
- **ネイティブ0x81(V→B)の `time=E_nat` はサーバーの次送信スケジュールそのもの**。記録しておけば
  「サーバーが次に送る保証時刻」が分かり、延長の目標（バックストップ）に使える。

### ネイティブの長ETAレコードによる上書き防止
横流し中もサーバーはB宛に長ETAのネイティブ0x81(V)を送る。後着採用なら**上書きで遅延が復活**しうる。
→ **横流し中はB宛ネイティブ0x81(V)を書き換え(edit)** して `time` を管理ETAに合わせる（or 抑制）。
ネイティブが無いtickには追記(inject)。edit と inject は「V→Bの0x81ストリームを一元管理する」1機構に統合。

### 2運用モード（トレードオフ）
| | A. 予測ETA(即送信) | B. ジッタバッファ(深さ1) |
|---|---|---|
| 方式 | 受信即inject、ETA=現tick+s_hat+margin | 次レコード受領後に前を送信、ETA=実到達tick |
| 遅延 | 最小(処理分のみ) | +1間隔(≈5tick/80ms) |
| 停止耐性 | margin/延長で緩和 | ジッタに**構造的に停止しない** |
| 速度再現 | s_hat精度依存 | 実間隔で正確 |

**推奨**: 基本は **A(予測ETA+margin+延長フォールバック)** で最小遅延。ジッタの大きいVは **B** に切替。
両モードとも「停止=緩やかな減速」プリミティブと「ネイティブ上書き防止」を共有。

### 正直な限界
利得は「**Vの近くに高頻度供給するピアが存在するか**」に完全依存。誰もVに近くなければ供給源が無く、
サーバー以上には改善不可 → その場合は**何もしない（悪化させない）**のが正解。

## 4.6 Dead reckoning（推測航法）— 供給不足時も滑らかに

§4.5の「直前poseのまま再送」は**劣化フォールバック**（等速車両が stutter する）。上位版は**外挿**:
供給レコードの位置・姿勢差分から速度を推定し、**ETAのtickでの予測poseを末端に入れて送る**。

- `v = Δpos/Δtick`（直近2供給から）＋**EMA平滑**、回転は角速度で外挿（最短弧に注意）。
- `ETA=間隔` にして予測poseを末端に置くと、等速なら **wall-time ETA で真値一致＝見かけ遅延ほぼゼロ**。
  単なる横流し（過去poseに到達）より本質的に上。加減速の誤差は **ETA=収束窓ぶんのlerpで吸収**。
- **外挿があれば間隔を伸ばしても滑らか** → 外挿 + 長間隔 = 滑らか かつ 低通信量。間隔＝マニューバ追従の速さツマミ。

### ログ実測での裏取り（`swcap vel`）
combatログで「1間隔先を等速外挿した予測位置の誤差」vs「据え置き誤差」を比較:

| 車両/条件 | hold p50 | dead-reckon p50 | 削減 | p90(hold→DR) |
|---|---|---|---|---|
| veh23 近距離(gap~5) | 0.896 u | **0.052 u** | **94%** | 2.08→0.21 |
| veh21 全体 | 0.747 u | **0.044 u** | **94%** | 1.86→0.18 |
| veh21 遠方ピア(gap~21) | 0.467 u | **0.049 u** | **90%** | 1.48→0.16 |

巡航速度≈0.15u/tick。**据え置き約0.9u誤差 → 外挿約0.05u**（桁で減）。残差は5tick lerpで吸収可能。**外挿は有効。**

### 実測で判明した必須ガード
- **不連続をまたいで外挿しない**: gap 225〜5300tick（リスポーン/テレポート/スポーンリセット）では誤差max=3827u
  でhold/DRとも外せない。→ **gap閾値で外挿を打ち切る（信頼度キャップ）** が必須。
- **速度推定は大gap由来サンプルを除外＋EMA平滑**（生サンプルにmax 28u/tickのテレポート外れ値あり=「速度ガタガタ」の実体）。

### マルチボディの扱い（重要な制約）
- **`bodyCount` は絶対に変えない**（過去検証: ボディ数変更でゲームが壊れる）。
- 外挿は **root(body0=type1) のみ**。非rootは最新供給値を据え置く。
- バイト削減したい場合のみ、非rootを **type 0x00（=変化なし）** にして送る（28B/body 削減）。優先度は低い。

## 4.7 通信量予算と可変リミット

### 目標（プレイスタイル）
- **近距離戦**（`session_20260913_002757`）と **長距離戦**（`session_20260913_005757_200`, 10km開始・2v3）が典型。
- **10km離れていても、近くに味方供給ピアがいる車両は位置遅れ <1秒**（チーム戦は各チームが固まるので成立）。
  誰からも遠く真値が高頻度に得られない車両は**諦める**。
- 人数は最大~10人、車両数も人数比で増える。サーバー上り **70 Mbps** に余裕を持って収める。**リミットは設定で可変**に。

### 実測帯域（`swcap bw`）
0x81は軽い: mean 74〜194B, **p50=55B**（単ボディ車両が多い）, max 838B。ネイティブ0x81総量 ≈ **0.18 Mbps**。
長距離戦での relay 投影（10人補正 ≒ 実質 O(人数²)、5→10人で約4倍）:

| relay間隔 | 遅延目安 | 5人実測 relay | 10人推定 relay | 対70Mbps |
|---|---|---|---|---|
| 5 t  | ~83ms  | 1.41 Mbps | ~5.6 Mbps | 8% |
| 10 t | ~167ms | 0.56 Mbps | ~2.2 Mbps | 3% |
| 20 t | ~333ms | 0.27 Mbps | ~1.1 Mbps | 1.6% |
| 30 t | ~500ms | 0.18 Mbps | ~0.4 Mbps | 0.5% |

### 結論
- **帯域は律速ではない**。interval=5（83ms遅延）でも10人で ~5.6 Mbps = 上りの **8%**。<1秒@10kmは余裕。
- 真の制約は帯域ではなく **供給ピアの有無**（＝諦めライン）。
- **可変リミットは安全弁**として設計する。予算超過時は dead reckoning があるので **優先度の低い（遠い/戦闘外）車両から
  interval を伸ばす** → 滑らかさは保ちマニューバ追従だけ落とす**優雅な劣化**。ハードにドロップするのは最終手段。

### 可変リミット設計（案）
- 設定値: **per-recipient 上限 Mbps**（既定は控えめ、例 5 Mbps）＋ 全体上限。回線の悪いピアは個別に下げられる。
- 関連度スコア = f(距離/native間隔, 戦闘関与, 移動中か)。**近い・速い車両ほど短い interval**、遠いほど長い interval。
- 予算内で interval を割り当て（近5t〜遠30t、上限でクランプ）。**relay間隔 = clamp(native間隔/k, floor=5, cap)** を基準に、
  予算超過なら floor/cap と k を締める。
- 測定ツール: `swcap bw <file> --interval N [--dense N] [--tps N]` で任意ログの予算を再算出可能。

## 4.8 段階A: シャドウ検証結果（`swcap shadow`）

キャプチャを**因果的に再生**（各時点で過去データのみ使用）し、遠方(受信B,車両V)ごとに
真値T(t)＝全ピア0x81の統合補間、Bの見た目Rnat(t)＝Bのネイティブ受信をクライアント補間モデルに通したもの、
横流し後Rrelay(t)＝freshな供給元からdead reckoningで生成した注入をモデルに通したもの、を再現して比較。

**10km戦（`session_20260913_005757_200`）:**

| interval | 送信間隔 | native 1秒以内 | relay 1秒以内 | relay遅延中央値 | 帯域(10人推定) |
|---|---|---|---|---|---|
| 5 t  | 83ms   | 33% | **99.8%** | 0ms  | 2.84 Mbps |
| 20 t | 333ms  | 16% | **99.8%** | 0ms  | 0.56 Mbps |
| 40 t | 667ms  | 16% | **99.7%** | 0ms  | 0.28 Mbps |
| 60 t | 1000ms | 14% | **98.9%** | 17ms | 0.16 Mbps |

- native = 中央値 **3.5秒遅れ**（位置誤差 509 units）。relay = **ほぼ0ms・99%が1秒以内**（誤差 1.5 units）。
- **dead reckoning により送信間隔を60t(1秒)まで伸ばしても遅延ほぼ0** → 「外挿+長間隔=滑らか かつ 激安」を実証。
- 近距離戦（`002757`）も native 25% → relay 98%、0.11 Mbps。
- **結論: 目標「10kmで<1秒」は大きなマージンで達成可能。実用は interval=20〜40 が遅延/帯域のバランス良好。**

> 補足: time-lag は探索窓上限（target×3）で頭打ち表示（nativeは実際にはそれ以上遅い）。sim は供給が `stale`
> 超なら注入をスキップ（信頼度キャップ）し native に委ねる。真値は同一サンプル由来なので relay 誤差はやや楽観側
> だが 509→1.5 の桁差は結論を揺るがさない。実フックの1tick処理遅延は無視できる。

## 4.9 段階B: 書き換え機構の安全確認（実装済み）

DLLに **C++版レコードウォーカー**（`src/hook/rec.h`, `Records.cs`の忠実移植）と、送信を変えない
**受動的自己テスト** + **可視スモークデモ（F7・既定OFF）** を実装。

### オフライン裏取り（実機前）
`tools/rectest/test_rec.cpp`（rec.h を .swcap に適用）を `swcap validate` と比較:

| ファイル | C++ rec.h full% | C# validate | 追記シミュ(ok/bad) |
|---|---|---|---|
| 10km戦 | 98.23% (71686) | 98.2% (71686) | 41163 / **0** |
| 近距離戦 | 99.47% (165328) | 99.5% (165328) | 40157 / **0** |
| S5 | 100% (3107) | 100% (3107) | 518 / **0** |

- DLL内ウォーカーがオフラインデコーダと**件数完全一致**（＝「理解できたメッセージだけ触る」ゲートが実機でも正しい）。
- 約82,000回の追記シミュレーション（recordCount++＆末尾追記→再walk）で**失敗ゼロ** → inject算術は安全。

### 実機ソロ確認手順（要実施）
```
1. server64.exe 起動 → build\inject.exe server64.exe → 接続
2. 車両1台をスポーンして目の前に置く
3. captures\session_*.log に "== selftest: ... full=(98〜100%) ... append ok=.. bad=0 ==" が定期出力されるか
4. F7 押下（"== MUTATE demo ON =="）→ 全車両rootが X+50 ずれて見える = 0x81編集がクライアントに届く確認 = 段階C GO
5. F7 で OFF。クラッシュ/desync が無いことを確認
```
- 実装ガード: mutationは `full`(自己テスト合格)なメッセージのみ、かつ**送信バッファのコピー**を編集して送る（呼び出し元バッファは不変）。
- ホットキー: F9=録画開始 / F10=停止 / F8=マーカー / **F7=mutationデモ切替(既定OFF)**。

## 4.10 段階C: 横流し本体（実装済み）

`src/hook/relay.h` に実装。`SendMessageToUser` が宛先ごとに呼ばれる利点で全ピアのストリームを観測し、
遠方の受信者へ dead-reckoning した 0x81 を追記する。

- **観測(受動)**: 全ピア横断の latest-0x81 キャッシュ（body0 pose + 2点速度、大gap除外）と、
  (peer,veh)→最終fresh tick（ネイティブ受信/自inject両方で更新）。
- **注入(F6, 既定OFF)**: 2パス。**距離判定**=ネイティブ受信間隔 `g_natGap` が interval超の車両だけ「管理」(近い車両は不干渉)。
  **中核原理**: ETAはサーバーの次回送出時刻の予言なので、**ETAは書き換えず**、そこに到達すべき **予測pose=predict(ETA−Λ)** を入れる。
  ETAが保持されるのでクライアントの約束は必ずサーバーの次送出で守られ**フリーズしない**、かつ末端が未来予測poseなので**正しい速度で滑走=遅延Λ**。Λ=目標遅延(既定5tick)。
  - パス1（**ネイティブpose書き換え**）: 遠方車両のネイティブ0x81の **ETAを保持し、pose(位置＋回転)を predict(E−Λ) に差し替え**（項目1の上位版）。
    回転は基底非依存の4D線形外挿+正規化（半球を揃えて最短弧、位置と同じ信頼度キャップ）（項目4）。
  - パス2（**追記**）: メッセージに無い管理車両を、**ETA=E_nat(サーバー次回送出時刻)** で追記。pose=predict(E_nat−Λ)。
    追記は「マニューバ補正の再ターゲット」であり、cadence=interval。cross-peer供給は速度推定を新鮮にするために使う。
  - full なメッセージのコピーにのみ実施。recordCount/total を整合。
  - **LOD(実装済, item2/3)**: (peer,車両)ごとに **I=native間隔/4 をclamp[5,60]、Λ=I/2** で選ぶ。
    近い車両=密I・小Λ(シャープ)、遠い車両=粗I・大Λ(滑らかだが遅延増、遠くて許容)。**per-recipient帯域予算**(既定2Mbps/人)を
    安全弁に、近い車両優先で予算内に収める。定数: `kIntMin/kIntMax/kLodDenser/kRelayMinGap/kCapKbpsPerPeer`。

### horizonクランプは不要（調査結論）
当初「E−Λが遠いと旋回で外挿が外れる」を懸念したが、実データ＋合成円運動で検証した結果:
- 誤差は**horizonではなく再ターゲット間隔(I)で決まる**（頻繁に再ターゲットすれば接線オーバーシュートは消える）。
- 合成円運動: 同期毎のみ再ターゲット(I=720)だと外側に+50オーバーシュート(=青正方形)だが、I=20で+0.4に激減。
- **Λは遅延専用ではなく、粗いIで出る接線オーバーシュートを打ち消す**。最適は **Λ≈I/2**（外側/内側が相殺）。
- max誤差~2200uはテレポート/リスポーン瞬間でネイティブと共通・外挿由来でない → 速度キャップは無効(むしろ悪化)。
→ よって horizonクランプも速度キャップも入れず、**IとΛのLODペア**（Λ=I/2）で制御する。

### LOD検証（shadow, 10km戦）
relay遅延中央値217ms・96.7%が1秒以内・帯域0.144Mbps（固定I=5の0.836から1/6）。near誤差17u / far誤差85u（=10km先0.5°）。walk失敗0。
- 既定パラメータ: interval=20t, stale=30t, gap外れ値>30t, 1メッセージ最大24レコード。

### 設定ファイル（`swhook.ini`）— config化済み
チューニング定数を `Cfg`(relay.h) に移し、プロジェクト直下 `swhook.ini` から実行時ロード。**F5で無再起動リロード**、
起動時とリロード時に有効値をログへ記録（実キャプチャに使用configが残る）。ファイル無し=ビルトイン既定で従来と完全一致。
- 形式: `key = value`（`key value`も可）、`#`/`;`はコメント、未知キーは無視（ログ）。
- 可変キー: `intervalMin` `intervalMax` `lodDenser` `lagRatio` `lagMin` `relayMinGap` `stale` `gapOutlier` `maxPerSend` `budgetWindow` `capKbpsPerPeer`。
- `lagRatio`=描画遅延Λをcadence I に対する割合で指定（`Λ = I × lagRatio`、既定0.5=従来のI/2）。`lagMin`=Λの下限tick（近距離車両は最低この遅延を維持）。
- **`capKbpsPerPeer` が可変帯域リミット**（宛先ごとkbit/s、近い車両優先なので下げると遠い車両から間引かれる）。
- `maxPerSend`/`kMaxRecLen` はバッファ確保のコンパイル時上限として残置（`maxPerSend`は`kMaxPerSend`にクランプ）。
- オフライン検証も可: `test_relay <cap> [config.ini]` でconfig適用して再walk（既定iniで従来値を再現、部分iniはデフォルトにマージ）。

### オフライン裏取り（`tools/rectest/test_relay.cpp`, 実機前）
実キャプチャを relay に流し、injectした全メッセージを再walk:

| ファイル | inject済msg | injectレコード | walk失敗 | recCount不整合 | 帯域 |
|---|---|---|---|---|---|
| 10km戦 | 50,522追記+6,824書換 | 57,346 | **0** | **0** | 0.125 Mbps |
| 近距離戦 | 11,214追記+3,062書換 | 14,276 | **0** | **0** | 0.028 Mbps |

約4万件の注入・書き換えメッセージすべてが完全に閉じる（クライアントのレコード列を壊さない）。帯域は `bw`/`shadow` と一致。

### バグ修正: 追記/書換ETAの遠方流用によるフリーズ（実機ログ 214246 で発見）
症状: プレイ中に機体が完全停止し復帰しない。
原因: pass1/pass2 とも deadline(time) に**サーバーの遠方ETA(g_natEta, 最大 tick+720t≒12秒)を流用**していた。
クライアントは「12秒後に着く遠い予測点」へ向けて等速で滑り続けるため、実機の動きに追従できず固まって見える。
test_relay 診断(注入列を再現し pose/ETA を検査)で確定: 追記ETA horizon が p50=220・**最大715tick**、ETA-poison 79件。
修正: deadline = **tick + I（自分の再送間隔）**、pose = predict(tick+I−Λ) に統一（pass1はネイティブETAを上書き、pass2も同様）。
密再送(LOD)では短い deadline が毎回更新されるためフリーズせず、外挿は I/2(≤30t)に有界。
検証: horizon 最大715→**60tick**、poison 79→6件(maxGap 64→11、ネイティブ自身のETA減少と同オーダーで無害)、walk失敗0・NaN無し。
※これは shadow --lod で検証したモデル(deadline=I先, Λ=I/2)そのもの。現行コードが g_natEta を使っていたのが逸脱だった。
test_relay に注入pose健全性診断(NaN/絶対値/quatノルム/対ネイティブjump/ETA-poison/horizon分布)を常設。

### 実機テスト計画
- **ソロ安定確認**: F6 ON でクラッシュ無し（ソロは1ピアなので注入はほぼ発生しない＝counters≈0 が正常）。
- **2人以上（本命）**: 1人が車両を遠方(数km〜10km)で操作、もう1人が観測。F6 OFF/ON を切替え、
  遠方車両の遅延が体感で改善するか確認。F9で OFF/ON 別々にキャプチャし `swcap shadow` 相当で事後比較も可。

## 4.11 約束モデルへの整理（2026-09-19, 実装済み）

§4.10 の LOD(I=native/4, Λ=I/2) と cadence 追記を、クライアント挙動から導いた「約束」モデルに置き換えた。

- **クライアント挙動**: レコード (pose, ETA) を受けると、現在描画位置から pose へ ETA ちょうどに到達するよう滑走し、
  ETA 後に何も来なければ停止。native は pose=P(T), ETA=T+I なので **遅延 = I**。
- **約束**: 受信者 B へ送る V の全レコード（native 書き換え／追記とも）を **pose = predict(ETA−Λ)** にする。
  定常状態では B は P(t−Λ) を真の速度で滑走 → **遅延 Λ（既定 5 tick）**。代償は予測水平線 h = ETA−Λ−sampleTick
  （旋回時の overshoot の源）。`horizonMax` で h をクランプし、超過分は Λ に回す。
- **ETA = 今 + I_src**: I_src は車両ごとの最密な native 間隔（全ピア横断、EMA）。ETA を native のまま据え置くと式の I が
  native のままになり Λ を下げられない（§4.5/§309 のフリーズは水平線無制限が原因で、クランプすれば据え置き方式も成立するが、
  密な源があるなら書き換えの方が Λ・h とも小さい）。`lodDenser` は費用上限に意味を変え、I = max(I_src, natGap/lodDenser)。
- **追記トリガ**: (a) B の最終約束より新しいサンプルがあり cadence I が経過、または (b) 最終約束の ETA が
  `kRefreshMargin`(3) tick 以内に切れる（源が止まっても古いサンプルで dead-reckon 継続）。`stale` 直前の最後の 1 発は
  ETA を native の次回送出時刻へ伸ばして native に引き継ぐ（源断絶＝近い人の切断でも停止しない）。
- **管理条件 `managed`**: 速度あり、age ≤ stale、natGap > relayMinGap、**I_src ≤ natGap × srcRatio**（源が受信者自身の
  feed より密でなければ何もしない：一人・遠方では native そのまま。粗い弦からの外挿は星型経路になる）。
- **不連続**: テレポート／リスポーンは unload+reload(0x2C/0x38) で届き、Veh 消去で速度がジャンプをまたがない。
  `gapOutlier` は unload を挟まないジャンプ用の予備で既定 0（無効）。
- **オフライン (test_relay)**: 近距離戦 walk失敗 0、追記 49.8k/0.138 Mbps（旧 11.2k/0.028）、水平線 p50=5；
  長距離戦 walk失敗 0、84.9k/0.226 Mbps、p50=7。ETA-poison 57/198 件(max 53t) は「急に近くなった車両」の類で従来同様。
- **開発ループ**: `swctl unload` → `build.bat` → `swctl inject` でサーバー無停止で DLL を差し替え可能（GUI は同一 pid に再注入しない）。

## 5. リスク・注意点

### 5.1 再送（resend）挙動
- クライアントが**連続しないワールド時刻のメッセージ**を受信すると、サーバーに再送要求を出す挙動が
  ある（前プロトコル観測）。再送は**過去メッセージを一気に送る**ため、そこへ挿入・改変しても無意味。
- **対策**: プレイヤー毎に「最新ワールド時刻」を記録し、**それより古いメッセージへの介入はスキップ**する。
- ⚠ 再送が **Steam層**で処理されている可能性もある（その場合この対策は無駄になるだけ）。
- **検証済み（`swcap resend`）**: 戦闘ログ2件（近距離`002757`／長距離`005757_200`）を精査。
  **宛先ストリームのワールドtickは厳密に前進**（backward step = 0 / 0）。つまりアプリ層に
  巻き戻り再送の痕跡は無く、再送はSteam層で処理されているとみられる。近距離戦は同一tick継続458件
  （回線の悪い1名の可能性）＋record単位の同一tick再出現68件・ETA後退2件があるが、いずれも
  envelope tickの前進を崩さない（1tick内の分割／同tick重複であり巻き戻りではない）。
- **実装済み（保険）**: `build_inject` 冒頭に **per-recipient last-processed-tick ガード**
  （`g_peerProc`）。宛先の最終処理tick以下のメッセージには介入せず素通し（return 0）。
  ログ上は発火しないが、副作用として**同一tick継続には最初の1通のみ介入**＝1tick内の二重注入を回避。
  test_relayでwalk失敗0を維持。

### 5.2 その他
- 横流しレコードの time が過去/未来すぎるとクライアントが無視 or 破綻する可能性 → §2の検証が前提。
- 追記でメッセージが肥大化した場合の断片化（bulk化）に注意。

## 6. 現状と次アクション

| フェーズ | 状態 |
|---------|------|
| ① キャプチャ（SendMessageToUserフック→.swcap） | ✅ 完成・稼働 |
| ② レコード長解読（バッチ歩行） | ✅ 全キャプチャ 99.4–100%、0x81長さ確定 |
| ③ 編集・注入（横流し実装） | ⏳ 準備完了・未着手 |

**③に入る前の検証タスク**:
1. ✅ **0x81.time の意味を実データで確定**（`time = tick + 次同期間隔`、near=+5、遠方=最大~687tick≈11秒。`swcap eta`）。
2. ✅ **遠方車両の実効同期間隔を実測**（combatでdelta 5〜687tick、~300tick≈5秒の遅延が多発 → 横流しの改善余地大）。
3. ⏳ `session_20260913_002757.swcap` で再送の痕跡（ワールドtickの飛び/巻き戻り）を確認。
   （`eta`のspacing欄に gap 0〜4tick の小さな塊＝同一車両の近接重複が68件など見えており、再送/重複の候補。要精査。）
