# 目的と設計 — 車両位置同期の改善

SW MultiSync が**何を解決し、どういう方針で解決しているか**をまとめる。
実装の入口は [src/hook/relay.h](src/hook/relay.h)、調整項目は [swhook.ini](swhook.ini)、
ワイヤ仕様は [protocol.md](protocol.md)、フックの仕組みは [hooking.md](hooking.md) を参照。

## 1. 目的

Stormworks のマルチプレイで、**自分から離れた車両の位置・姿勢が数秒遅れて表示される**問題を、
**専用サーバー側に入れるだけ**（参加者側の導入なし・ゲーム本体の改変なし）で改善する。

対戦系のプレイ（10km 級の長距離戦を含む）で、味方の近くにいる車両は 10km 先からでも
**1 秒未満の遅れ**で見えることを目標とする。

## 2. 解決したい問題（クライアントの挙動モデル）

キャプチャ解析（`swcap eta` / `vel` 等）で確定した事実:

- サーバーはワールド tick ごとに各プレイヤー宛の同期メッセージ（ch0 `msgType=8`）を送る。
  この中に**間欠的に**車両同期レコード `0x81` が載る（[protocol/sync.md](protocol/sync.md)）。
- `0x81` の同期頻度は **(宛先プレイヤー, 車両) ごとに独立**で、近い車両ほど高頻度（5 tick）、
  遠い車両ほど低頻度（最大 ~700 tick ≒ 11 秒）。tick rate ≈ 62.5/s。
- `0x81.time` は **未来のワールド tick（ETA）** で、常に `time = 送信tick + 次の同期までの間隔`。
  クライアントは「現在の描画位置から、レコードの pose へ **ETA ちょうどに到達するよう滑走**」し、
  **ETA を過ぎて次が来なければその場で停止**する（レコードに速度は無く、クライアントは外挿しない）。
- したがって native の表示遅延は同期間隔そのもの。5 秒間隔でしか同期されない車両は
  **5 秒遅れの位置**に見える。

## 3. 中核アイデア — クライアント間の横流し（cross-feed）と「約束」モデル

### 3.1 なぜサーバー側で可能か
`SendMessageToUser` は**宛先ごとに 1 回ずつ**呼ばれるので、フックは**全プレイヤー宛の全ストリームを
1 プロセス内で観測**できる。ある車両 V に近いプレイヤー A 宛のストリームには、V の最新 pose が
5 tick ごとに流れている。これを遠いプレイヤー B 宛のストリームに流用する。

### 3.2 約束モデル（実装の中核）
クライアントの挙動を「(pose, ETA) という**約束**を受け取り、それを守るよう滑走する」と捉えると、
native は `pose = P(T), ETA = T + I`（I = 同期間隔）なので**遅延 = I**。これを次のように置き換える:

- B へ送る V の**全レコード**（native の書き換え／追記の両方）を
  **`pose = predict(ETA − Λ)`、`ETA = 今 + I_src`** にする。
  - `predict(t)` = 全ピア横断で得た V の最新サンプルと速度（位置は線形、回転は 4D 線形外挿+正規化）からの推測航法。
  - `I_src` = V の最も密な native 間隔（全ピア横断、EMA）。`Λ` = 目標描画遅延（既定 5 tick）。
  - コスト上限として `I ≥ natGap / lodDenser`（B の native 間隔の 1/8 より密にはしない）。この
    `natGap` は実測値だけでなく、**サーバーの距離 LOD 則 `0.1·d − 6`（5..720 tick、protocol/sync.md）**
    を B の位置（client `0x2F`）と V の予測位置（今と次回約束時点の近い方）で評価した値との **min**
    を取る（`distLod`、既定 ON）。サーバーは間隔を送出時にしか決め直さないので、急接近する車両の
    実測 natGap はしばらく大きいままになる — min は「密にする」方向にしか働かず、その遅れだけを埋める。
- 定常状態で B は `P(t − Λ)` を**真の速度で滑走**する → **遅延 Λ（≒80ms）**、速度の見た目も正しい。
- 代償は予測水平線 `h = ETA − Λ − sampleTick`（旋回時のオーバーシュートの源）。`horizonMax` で h を
  クランプする。ただし上限は **`max(horizonMax, 2·I_src + margin)`**: 同じサンプルから出す 2 発目の
  約束は h ≈ 2·I_src になるので、これより低くクランプすると連続する約束の target が同じ点に固定され、
  クライアントは一度そこへ滑走した後**次のサンプルまで停止する**（遠方のミサイルがカツカツ動く不具合、
  2026-09-19）。`horizonMax` が効くのは源が止まった時（age > I_src）の dead-reckoning の有界化。
  ただし源の間隔が 2·I_src を大きく超える場合（発射側から離れていくミサイル: 源の間隔 356 tick に対し
  上限 123）も同じ固定が起きて、撃たれる側の最接近点で機体が止まる（2026-09-22）。dead-reckoning の
  実質的な上限は `stale` が担うので、**`horizonMax` の既定値は `stale` と同じ 300** にした。

### 3.3 送出制御
- **管理条件 (`managed`)**: 速度が推定済み・サンプル鮮度 ≤ `stale`・B の native 間隔 > `relayMinGap`・
  **`I_src ≤ native間隔 × srcRatio`**（源が B 自身の feed より密でなければ何もしない）。
- **パス 1（書き換え）**: B 宛メッセージ内の native `0x81` の pose を `predict(ETA−Λ)` に差し替える。
- **パス 2（追記）**: メッセージに無い管理車両を、(a) 前回の約束より新しいサンプルがあり cadence I が
  経過、または (b) 前回の約束の ETA が間もなく切れる、のいずれかで追記する（源が止まっても
  古いサンプルで dead-reckoning を継続）。
- **native への引き継ぎ**: `stale` 直前の最後の 1 発は ETA をサーバーの次回送出時刻へ伸ばして送る。
  源（近くのプレイヤー）が切断しても停止しない。
- **帯域の安全弁**: 宛先ごとに `capKbpsPerPeer` の予算を持ち、近い車両を優先して予算内に収める。
  超過時は遠い車両から間引く（優雅な劣化）。

## 4. 設計原則（不変条件）

実装・変更時に守ること。いずれも実データやトラブルから得た結論。

1. **新しいメッセージを作らず、そのtickで自然に出ていくメッセージへ追記する。**
   ワールド時刻の連続性を崩さないため。`recordCount` と本体長を整合させる。
2. **完全に walk できたメッセージにしか触らない。** レコード長ルールが全て分かり、
   本体をぴったり消費できたメッセージ（`rec.h`、`swcap validate` と同じ判定）だけを編集対象にする。
3. **呼び出し元のバッファは変更せず、コピーを編集して送る。**
4. **tick 計算はエンベロープの world-tick 差分のみで行う。** tps は負荷で落ち込むので
   リアル時間を ETA 計算に使わない。リアル時間はタイムアウト等の例外検知のみ。
5. **`bodyCount` は絶対に変えない**（変えるとゲームが壊れる）。外挿は root（body0）のみ、
   非 root は最新値を据え置く。
6. **約束の ETA は短く保つ。** deadline に遠方の native ETA（最大 ~12 秒先）を流用すると、
   クライアントが遠い予測点へ向かって滑り続け**フリーズして見える**（実機で発生した不具合）。
   ETA は常に `今 + I_src`、水平線は `max(horizonMax, 2·I_src + margin)` で有界にする。
7. **悪化させない。** 近くに高頻度の供給ピアがいない車両は native のまま（何もしない）。
   粗い弦からの外挿は星型の経路になるだけで改善にならない。
8. **宛先ごとの最終処理 tick 以下のメッセージには介入しない**（再送・同一 tick 重複への保険。
   アプリ層での巻き戻り再送は観測されておらず、Steam 層で処理されているとみられる）。
9. **不連続をまたいで外挿しない。** テレポート／リスポーンは unload+reload（0x2C/0x38）で届くので、
   車両履歴をそこでリセットする。`gapOutlier` は unload を挟まないジャンプ用の予備。
10. **運転時の制御は全て IPC 経由**（[ipc-protocol.md](ipc-protocol.md)）。ホットキー等の別経路を作らない。

## 5. 限界

- 改善量は「**V の近くに高頻度で同期されているピアがいるか**」に完全に依存する。誰も近くにいない車両は
  サーバー以上には改善できない（チーム戦では各チームが固まるので実用上は成立する）。
- 帯域は律速ではない（native の 0x81 総量 ≈ 0.2 Mbps、relay 追加分は 10 人でも数 Mbps 以下）。
  `capKbpsPerPeer` は回線の悪いピア向けの安全弁。
- 見かけの遅延 Λ と旋回時の精度（水平線 h）はトレードオフ。粗い源ほど遅延側に倒す。

## 6. 裏付けとなる検証結果（要約）

| 検証 | 結果 |
|---|---|
| `0x81.time` の意味（`swcap eta`） | delta = time − tick は常に ≥ 0。近距離は全件 +5 で実間隔も 5 tick。遠方は 5〜687 tick で実間隔分布と一致 |
| 推測航法の有効性（`swcap vel`） | 1 間隔先の位置誤差 p50: 据え置き ≈0.9u → 等速外挿 ≈0.05u（約 94% 減）。ただしリスポーン等の大 gap は外挿不可 |
| 因果シミュレーション（`swcap shadow`、10km 戦） | native は中央値 3.5 秒遅れ（誤差 509u）→ relay は遅延ほぼ 0・99% が 1 秒以内（誤差 1.5u）。間隔 60t でも成立 |
| 帯域（`swcap bw`） | 0x81 は p50=55B。relay 間隔 5t・10 人推定でも ≈5.6 Mbps（上り 70 Mbps の 8%） |
| 書き換え機構の安全性（`tools/rectest`） | DLL 内 walker とオフラインデコーダの件数が完全一致。実キャプチャへの注入 ~13 万件で walk 失敗 0・recordCount 不整合 0・NaN 無し |
| サーバーの距離 LOD（`test_dist`、5 人戦闘 2026-09-19） | 0x81 の dETA は受信者↔車両距離 d で決まり `≈ 0.1·d − 6` tick（110 m 以内 5、12 s 上限）、速度非依存。約束破り（実間隔 > dETA+2）は 0.6%、ほぼ静止車両の送信省略とサーバーのヒッチ。タイル跨ぎとは無相関 |
| 再送痕跡（`swcap resend`） | 宛先ストリームの world tick は厳密に前進（巻き戻り 0）。同一 tick 継続はあるが前進を崩さない |

## 7. 関連ドキュメント

- [README.md](README.md) — 利用者向け概要・ビルド・使い方
- [hooking.md](hooking.md) — どこをどうフックしているか
- [protocol.md](protocol.md) / [protocol/](protocol/) — サーバー→クライアント プロトコル解析
- [ipc-protocol.md](ipc-protocol.md) — 制御プレーン仕様
- [tools/swcap/README.md](tools/swcap/README.md) — キャプチャ解析プレイブック
- [AGENTS.md](AGENTS.md) — コーディングエージェント向けコンテキスト
