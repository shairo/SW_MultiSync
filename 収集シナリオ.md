# 収集シナリオ（単独プレイで再現可能な分）

未解読の希少タグを、狙って発生させて採取するための手順集。協力者が必要な複数人シナリオは
末尾に「保留」として分離。目的はあくまで server→client レコードの解読 → 編集/追記。

## 共通ワークフロー（ホットキーで録画制御 — サーバー再起動不要）

DLLは常駐フックなので、**一度注入すれば以降プレイ中にキーで録画を制御**できる。
シナリオごとにサーバーを出入りする必要はない。

1. `server64.exe` を起動
2. `build\inject.exe server64.exe` で `swhook.dll` を注入（接続前に一度だけ）
3. クライアントで接続
4. **各シナリオ**を、以下のキーで区切りながら連続実施:

   | キー | 動作 |
   |------|------|
   | **F9** | 録画開始（**新しい `.swcap` ファイルを切る** → 1シナリオ1ファイル） |
   | **F10** | 録画停止（flush＋close） |
   | **F8** | マーカー挿入（「今スポーンした」等の目印） |

   例: `F9` → S1の操作 → `F10` … `F9` → S2の操作 → `F10` …
5. 全部終わったらサーバー終了（DLL解放。再ビルド時のみ必要）

> - 注入直後はデフォルト録画ON（従来互換）。狙って区切るなら最初に `F10` で止めてから `F9`。
> - F8/F9/F10 はパッシブ読み取りなので**ゲーム側にも同時入力される**。既定バインドと衝突するなら
>   `src/hook/dllmain.cpp` の `kKeyStart/kKeyStop/kKeyMark` を変更して再ビルド。
> - リビルドは server 停止中のみ可（DLLが掴まれているとLNK1104）。

### 採取後の確認
```
swcap marks <file>            # 打ったF8マーカーの t+ms 一覧
swcap when  <file> --tag N    # タグNの発火 t+ms → マーカー/操作と照合
```

## 採取のコツ（タグを操作に確実に対応させる）

- **1キャプチャ＝1種類の操作**に絞る。混ぜると対応付けが濁る。
- 各操作は **「5秒静止 → 操作 → 5秒静止」を3回**繰り返す。周期タグは常時出るが、
  狙いのタグは操作時刻にだけ固まって出る → 区別できる。
- **操作した実時刻をメモ**（例: 接続後およそ何秒で何をしたか）。キャプチャ先頭からの相対msを
  `swcap when --tag N` が出すので突き合わせられる。
- まず **S0（何もしない基準）** を採り、常時出る周期タグ（168/142/81/05/166/167/39…）を把握。
  以降のシナリオで「基準に無く操作時に出た」タグが本命。

## 検証コマンド

```
swcap census <file>            # タグ別 回数・初出/最終tick・分布（START/END/spread）
swcap when   <file> --tag N    # タグNの全発火（tick, t+ms, seq）→ 操作メモと照合
swcap walk   <file> --tag N    # そのタグを含む最初のメッセージのレコード内訳＋未解読テール
swcap validate <file>          # 全体の解読率（新タグ実装後の回帰確認）
```

---

## シナリオ一覧（単独）

### S0 — 基準（無操作）
- **操作**: 接続して30秒、キャラを一切動かさず静止。車両なし。
- **狙い**: 常時発生する周期タグの基準化。以降の「差分」判定に使う。

### S1 — 車両スポーン
- **操作**: ワークベンチ/エディタから車両を1台スポーン。3回（毎回デスポーンしてから）。
- **狙いタグ**: `0x4D(77)` 全状態初期同期, `0x29(41)` equip, `0x47(71)` 直後バースト。
- **メモ**: 既存キャプチャで 77 はスポーンtickに4連、71 はその直後に5連を観測済み。裏取り。

### S2 — 車両デスポーン
- **操作**: スポーン済み車両を1台削除（リセット/despawn）。3回。
- **狙いタグ**: `0x7B(123)`（終盤に単発観測 → 離脱かデスポーンか切り分け）, 車両削除通知系。
- **メモ**: S1と対で、スポーン/デスポーンのどちらがどのタグか確定させる。

### S3 — 乗降（シート）
- **操作**: 車両のシートに座る→降りる。3往復。
- **狙いタグ**: 座席/キャラ装着系（71 が乗車関連なら再現するはず）。

### S4 — インベントリ装備
- **操作**: 装備アイテム（消火器・ライト等）を手に持つ→しまう。3回。
- **狙いタグ**: `0x29(41)` equip, 手持ち状態同期系。

### S5 — 運転（操作入力）
- **操作**: 車両に乗り前進/後退/旋回を混ぜて30秒運転。
- **狙いタグ**: `0x1A(26)`（中盤=運転中のみ活性の準周期）, `0x81` type2 の増加。
- **メモ**: 26 が運転入力に連動するか（速度/操舵と相関）を when で確認。

### S6 — 車両ボタン/スイッチ操作
- **操作**: 車両上の押しボタン/トグルスイッチ/レバーを操作。各3回。
- **狙いタグ**: creation状態同期（20/21/43/45/85/97 のいずれか）。
- **メモ**: `0x15(21)`+`0x61(97)` は同一tick域でペア観測 → ボタン系イベントの候補。

### S7 — 地図マーカー/マップ操作
- **操作**: マップを開きマーカーを設置→削除。3回。
- **狙いタグ**: マーカー/UI同期系（`0x88(136)`, `0x8B(139)` の単発候補）。

### S8 — Popup（アドオン経由）
- **操作**: `setPopupScreen` 以外のPopupコマンド（`setPopup` 位置追従, `removePopup`）を
  簡単なアドオンLuaで発火。数回。
- **狙いタグ**: `0x8E(142)` の別サブタイプ（show=0 の消去、位置追従版など）。
- **メモ**: 142 は length規則確定済み。ここはサブタイプ差分の確認で、編集デモの下地。

### S9 — キャラ状態（ダメージ/死亡/リスポーン）
- **操作**: 落下ダメージ→死亡→リスポーン。2回。
- **狙いタグ**: キャラHP/状態同期系（`0x09(9)`, `0x62(98)`, `0x55(85)` の散発候補）。

### S10 — 環境変更
- **操作**: 設定から時刻/天候を変更、または長時間放置で時刻進行。
- **狙いタグ**: `0x05(5)`（周期・clock/environment候補）の値変化, 時間同期系。

---

## 保留（協力者が必要 = 複数人）

- **P1** 他プレイヤーの参加/離脱通知
- **P2** 他プレイヤーのアバター位置/姿勢同期
- **P3** チャット/ボイス関連
- **P4** 他プレイヤー視点のPopup配信

これらは相手の入退室・移動が要るため単独では出せない。単独シナリオで基本タグを固めてから、
協力者ありのセッションで差分として抽出する。

---

## 進め方の推奨

1. まず **S0 → S1 → S2** を採り、スポーン/デスポーンのタグを確定（既存観測の裏取り）。
2. `swcap census` で各キャプチャのタグ構成を比較、`when` で操作時刻と照合。
3. 新タグの長さが分かり次第 `tools/swcap/Records.cs` に規則追加 → `validate` で回帰確認
   （オーバーラン0を維持できるかが正しさの担保）。
4. 解読が進んだら [protocol.md](protocol.md) に追記。
