# 変更履歴

## v0.2.2

### 同期改善（relay）
- 遠くを飛ぶミサイルなどの車両が、数秒止まってから一気に進む「コマ送り」になる問題を修正しました。サーバーからの更新間隔が長い車両でも、途切れずに動き続けます。
- 参加者と車両の距離に応じて、同期のきめ細かさを調整するようにしました。近づいてくる車両は、ゲーム本来の更新頻度が上がるより先に、細かく同期されるようになります（`distLod`、既定で有効）。
- 設定項目 `horizonMax` を廃止しました。古い swhook.ini に書かれていても無視されるので、そのまま使えます。

### 新機能
- **ゲーム内から稼働状態を確認できるようになりました**: チャットで `?multisync` と送ると、送った人にだけ swhook のバージョンと同期改善の ON/OFF が返ってきます。
- **プレイヤー名を表示するようにしました**: 参加時の情報（取れなかった場合はチャット）からゲーム内のプレイヤー名を取得し、GUI と swctl に表示します。
- **参加者ごとの遅延を表示するようにしました**: クライアントが申告するフレーム遅延（ゲーム内プレイヤー一覧の「+Nf」）、サーバーとの tick 差、クライアント側の TPS を、状態タブと swctl に表示します。
- **ゴーストチャンク対策（実験的）**: 特定の場所で参加者の画面が止まる不具合への実験的な対策を追加しました。効果は保証しません。詳しくは `README_実験機能_unstuck.txt` を参照してください。

### 修正
- 日本語などの ASCII 以外の文字を含むフォルダーに置くと動かなかった問題を修正しました（ログ・録画のパスの文字化け、「録画フォルダーを開く」の失敗、日本語以外の環境で DLL を組み込めない問題）。
- 「開発者向けデバッグタブを表示する」を切り替えると、「起動・注入」タブの表示が崩れる問題を修正しました。

### 開発者向け
- クライアントの画面が止まる状態を検知する処理と、その調査用の操作（通信の一時遮断、テレポート、タイル再送など）をデバッグタブに追加しました。
- 通信の解析範囲を広げ、プレイヤー参加時のメッセージなども解析できるようにしました。
- 使われていなかった「フレーム巻き戻り数」の表示を削除しました。
